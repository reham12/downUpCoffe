import { StyleSheet} from 'react-native'

export default StyleSheet.create({
  ScrollView:{
    justifyContent:"space-between",
    backgroundColor: '#fff',
    flex:1,
    paddingTop:30,
    paddingBottom:10,
  },
});